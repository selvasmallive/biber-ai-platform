{"instruction":"Return only Rust code. Define a public Payment struct with public fields from, to, amount, and nonce. Also define a free function validate_payment(payment: &Payment) -> Result<(), String> that rejects blank addresses and zero amount.","output":"pub struct Payment {\n    pub from: String,\n    pub to: String,\n    pub amount: u64,\n    pub nonce: u64,\n}\n\npub fn validate_payment(payment: &Payment) -> Result<(), String> {\n    if payment.from.trim().is_empty() {\n        return Err(\"from address is required\".to_string());\n    }\n    if payment.to.trim().is_empty() {\n        return Err(\"to address is required\".to_string());\n    }\n    if payment.amount == 0 {\n        return Err(\"amount must be greater than zero\".to_string());\n    }\n    Ok(())\n}","category":"rust","stack":["rust","xriq","blockchain"],"quality":"verified"}
{"instruction":"Return only Rust code. Define a public LedgerHeader struct with public fields height, previous_hash, and timestamp_ms. Also define a free function next_ledger_height(parent: &LedgerHeader) -> Option<u64> using checked_add.","output":"pub struct LedgerHeader {\n    pub height: u64,\n    pub previous_hash: [u8; 32],\n    pub timestamp_ms: u64,\n}\n\npub fn next_ledger_height(parent: &LedgerHeader) -> Option<u64> {\n    parent.height.checked_add(1)\n}","category":"rust","stack":["rust","xriq","blockchain"],"quality":"verified"}
{"instruction":"Return only Rust code. Define pub fn storage_fee(byte_count: usize, fee_per_byte: u64) -> Option<u64> that converts byte_count to u64 and uses checked multiplication.","output":"pub fn storage_fee(byte_count: usize, fee_per_byte: u64) -> Option<u64> {\n    let byte_count_u64: u64 = byte_count.try_into().ok()?;\n    byte_count_u64.checked_mul(fee_per_byte)\n}","category":"rust","stack":["rust","xriq","blockchain"],"quality":"verified"}
{"instruction":"Return only Rust code. Using std::collections::HashSet, define pub fn remember_transaction(seen: &mut HashSet<String>, tx_id: String) -> bool that returns true only for a new transaction id.","output":"use std::collections::HashSet;\n\npub fn remember_transaction(seen: &mut HashSet<String>, tx_id: String) -> bool {\n    seen.insert(tx_id)\n}","category":"rust","stack":["rust","xriq","blockchain"],"quality":"verified"}
{"instruction":"Return only Rust code. Define a public tuple struct CoinAmount(u64) with constructors and checked arithmetic: new, as_u64, checked_add, and checked_sub.","output":"#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]\npub struct CoinAmount(u64);\n\nimpl CoinAmount {\n    pub fn new(value: u64) -> Self {\n        Self(value)\n    }\n\n    pub fn as_u64(&self) -> u64 {\n        self.0\n    }\n\n    pub fn checked_add(self, other: Self) -> Option<Self> {\n        self.0.checked_add(other.0).map(Self)\n    }\n\n    pub fn checked_sub(self, other: Self) -> Option<Self> {\n        self.0.checked_sub(other.0).map(Self)\n    }\n}","category":"rust","stack":["rust","xriq","blockchain"],"quality":"verified"}
{"instruction":"Return only Rust code. Define pub fn parse_coin_amount(input: &str) -> Result<u64, String> that trims whitespace, rejects empty input, and parses a u64.","output":"pub fn parse_coin_amount(input: &str) -> Result<u64, String> {\n    let trimmed = input.trim();\n    if trimmed.is_empty() {\n        return Err(\"amount is required\".to_string());\n    }\n    trimmed\n        .parse::<u64>()\n        .map_err(|_| \"amount must be a non-negative integer\".to_string())\n}","category":"rust","stack":["rust","xriq","blockchain"],"quality":"verified"}
{"instruction":"Return only Rust code. Define pub fn debit_balance(balance: u64, amount: u64) -> Result<u64, String> that uses checked_sub and returns an error when funds are insufficient.","output":"pub fn debit_balance(balance: u64, amount: u64) -> Result<u64, String> {\n    balance\n        .checked_sub(amount)\n        .ok_or_else(|| \"insufficient funds\".to_string())\n}","category":"rust","stack":["rust","xriq","blockchain"],"quality":"verified"}
{"instruction":"Return only Rust code. Using std::collections::HashMap, define pub fn get_balance(balances: &HashMap<String, u64>, account: &str) -> u64 that returns zero for missing accounts.","output":"use std::collections::HashMap;\n\npub fn get_balance(balances: &HashMap<String, u64>, account: &str) -> u64 {\n    balances.get(account).copied().unwrap_or(0)\n}","category":"rust","stack":["rust","xriq","blockchain"],"quality":"verified"}
{"instruction":"Return only Rust code. Define a public SignedTransaction struct with fields id, signer, payload_hash, and signature. Also define a free function has_signature(tx: &SignedTransaction) -> bool that checks signer and signature are not blank.","output":"pub struct SignedTransaction {\n    pub id: String,\n    pub signer: String,\n    pub payload_hash: [u8; 32],\n    pub signature: Vec<u8>,\n}\n\npub fn has_signature(tx: &SignedTransaction) -> bool {\n    !tx.signer.trim().is_empty() && !tx.signature.is_empty()\n}","category":"rust","stack":["rust","xriq","blockchain"],"quality":"verified"}
{"instruction":"Return only Rust code. Define pub fn validate_nonce(expected: u64, actual: u64) -> Result<(), String> that succeeds only when actual equals expected.","output":"pub fn validate_nonce(expected: u64, actual: u64) -> Result<(), String> {\n    if actual == expected {\n        Ok(())\n    } else {\n        Err(format!(\"invalid nonce: expected {expected}, got {actual}\"))\n    }\n}","category":"rust","stack":["rust","xriq","blockchain"],"quality":"verified"}
{"instruction":"Return only Rust code. Include the necessary use std::collections::HashSet import. Define pub fn insert_tx(mempool: &mut HashSet<String>, tx_id: String) -> bool that returns true only when tx_id was not already present.","output":"use std::collections::HashSet;\n\npub fn insert_tx(mempool: &mut HashSet<String>, tx_id: String) -> bool {\n    mempool.insert(tx_id)\n}","category":"rust","stack":["rust","xriq","blockchain"],"quality":"verified"}
{"instruction":"Return only Rust code. Include the necessary use std::collections::HashSet import. Define pub fn mark_seen_nonce(seen: &mut HashSet<u64>, nonce: u64) -> bool that records a transaction nonce and returns true only for a new nonce.","output":"use std::collections::HashSet;\n\npub fn mark_seen_nonce(seen: &mut HashSet<u64>, nonce: u64) -> bool {\n    seen.insert(nonce)\n}","category":"rust","stack":["rust","xriq","blockchain"],"quality":"verified"}
{"instruction":"Return only Rust code. Include the necessary use std::collections::HashSet import. Define pub fn register_validator(registered: &mut HashSet<String>, validator_id: String) -> Result<(), String> that rejects blank validator ids and duplicate registrations.","output":"use std::collections::HashSet;\n\npub fn register_validator(\n    registered: &mut HashSet<String>,\n    validator_id: String,\n) -> Result<(), String> {\n    if validator_id.trim().is_empty() {\n        return Err(\"validator id is required\".to_string());\n    }\n    if registered.insert(validator_id) {\n        Ok(())\n    } else {\n        Err(\"validator is already registered\".to_string())\n    }\n}","category":"rust","stack":["rust","xriq","blockchain"],"quality":"verified"}
{"instruction":"Return only Rust code. Include the necessary use std::collections::HashSet import. Define pub fn add_peer(peers: &mut HashSet<String>, peer_addr: &str) -> Result<bool, String> that trims the peer address, rejects empty addresses, and returns whether the peer was newly added.","output":"use std::collections::HashSet;\n\npub fn add_peer(peers: &mut HashSet<String>, peer_addr: &str) -> Result<bool, String> {\n    let peer_addr = peer_addr.trim();\n    if peer_addr.is_empty() {\n        return Err(\"peer address is required\".to_string());\n    }\n    Ok(peers.insert(peer_addr.to_string()))\n}","category":"rust","stack":["rust","xriq","blockchain"],"quality":"verified"}
{"instruction":"Return only Rust code. Include the necessary use std::collections::HashSet import. Define pub fn record_known_block(known_blocks: &mut HashSet<[u8; 32]>, block_hash: [u8; 32]) -> bool that returns true only when the block hash was not already known.","output":"use std::collections::HashSet;\n\npub fn record_known_block(\n    known_blocks: &mut HashSet<[u8; 32]>,\n    block_hash: [u8; 32],\n) -> bool {\n    known_blocks.insert(block_hash)\n}","category":"rust","stack":["rust","xriq","blockchain"],"quality":"verified"}
{"instruction":"Return only Rust code. Include the necessary use std::collections::HashSet import. Define pub fn remove_confirmed_tx(mempool: &mut HashSet<String>, tx_id: &str) -> bool that removes a confirmed transaction id and returns whether it existed.","output":"use std::collections::HashSet;\n\npub fn remove_confirmed_tx(mempool: &mut HashSet<String>, tx_id: &str) -> bool {\n    mempool.remove(tx_id)\n}","category":"rust","stack":["rust","xriq","blockchain"],"quality":"verified"}
